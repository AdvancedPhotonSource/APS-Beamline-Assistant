# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.venv/
venv/
env/

# User config (contains credentials!)
.env
*.env
!.env.template

# User data
conversation_history.json
*.log

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
~$*
.project
.pydevproject

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Data files (too large for git)
*.tiff
*.tif
*.ge2
*.h5
*.hdf5
*.zarr
uploads/
example-tif/

# Temporary and test files
test.py
debug*.py
*-old/
*-backup/
*-v1.py
*-stable.py
temp/
tmp/
*.tmp

# Large PDFs
*.pdf

# Knowledge Base - User Documents (private data, too large)
knowledge_base/papers/*.pdf
knowledge_base/logbooks/*.txt
knowledge_base/logbooks/*.md
knowledge_base/books/*.pdf
# NOTE: knowledge_base/chroma_db/ is intentionally NOT ignored — the prebuilt
# index (~15 MB) ships in git so an air-gapped beamline machine has a working RAG
# on checkout with no re-index step. (.DS_Store inside it is still caught by the
# global macOS rule above.) Re-emit + recommit after changing any source doc:
#   uv run python knowledge_base/index_knowledge.py

# Knowledge Base - Keep tracked files
!knowledge_base/data/*.json
!knowledge_base/*.py
!knowledge_base/README.md

# Knowledge Base - ship the open MIDAS reference docs (public, from the MIDAS repo)
# so the RAG KB source travels with a fresh checkout. The logbooks/*.md blanket
# ignore above still protects the drop-zone for any proprietary beamtime notes an
# operator adds. Add a line here when a new open MIDAS doc is added to the KB.
!knowledge_base/logbooks/FF_HEDM_Handbook.md
!knowledge_base/logbooks/FF_HEDM_Lab_Notebook.md
!knowledge_base/logbooks/NF_HEDM_Handbook.md
!knowledge_base/logbooks/NF_HEDM_Lab_Notebook.md
!knowledge_base/logbooks/Reconstruction_Reports.md
!knowledge_base/logbooks/FF_Analysis.md
!knowledge_base/logbooks/FF_Interactive_Plotting.md
!knowledge_base/logbooks/FF_autocalibrate.md
!knowledge_base/logbooks/FF_dual_datasets.md
!knowledge_base/logbooks/ForwardSimulationManual.md
!knowledge_base/logbooks/MIDAS_RADINT.md
!knowledge_base/logbooks/NF_Analysis.md
!knowledge_base/logbooks/NF_calibration.md
!knowledge_base/logbooks/NF_gui.md
!knowledge_base/logbooks/PF_Analysis.md

# Compiled files
*.pyc
*.pyo

# Documentation build
site/
.cache/

# Development and internal documentation
ADDING_NEW_SERVERS.md
BEAMLINE_DEPLOYMENT.md
MODULAR_ARCHITECTURE.md
MIGRATION_STRATEGY.md
CHECK_ATTRIBUTION.md
INSTALL_GH_CLI.md
ESSENTIAL_FILES.txt

# Old versions and alternatives
basic-client.py
fastmcp_midas_server.py
simple_midas_server.py
main.py
midas_workflows.py

# Test and validation scripts
test_*.sh
test_*.py
run_midas_validation.py
validate_midas_setup.py

# Debug and development scripts
debug_startup.sh
startup-mac.sh
startup.sh

# Test data and test directories
*.dat
test*/
test-*/
!tutorials-demo/

# Manuscripts (not for public repo) — each subdirectory is its own repo with
# its own Overleaf sync (1-CS-APEXA-Bench, 2-NMI-APEXA, ...).
manuscripts/

# MIDAS output artifacts
*.ge1
*.ge2
*.ge3
*.ge4
*.ge5
*.ge1.sum
*.bin
*.zip
*.corr.csv
*_trace_iter*.csv
*convergence_history.csv
*ps.txt
ps.txt
ps_init_sim.txt
refined_MIDAS_params.txt
refined_MIDAS_params_CeO.txt
ci_profiles.csv
autocal.log

# Parameter files (user data)
Parameters.txt
Parameters_CeO2_7m.txt

# Frontend: node_modules rebuilt locally; dist IS committed so the Web UI runs
# without Node/npm (Windows/demo boxes). Rebuild + recommit after frontend edits.
# Negation overrides the generic `dist/` rule above so frontend/dist stays tracked.
frontend/node_modules/
!frontend/dist/
!frontend/dist/**

# Misc
paraview_export.py
site/
ARGO-API-Docs/
test_viewer.html

# Materials Project CIF downloads to working dir
CeO2_*.cif
Fe_*.cif
LaB6_*.cif
Ni_*.cif
Si_*.cif
Ti_*.cif
cif_files/
cifs/

# Calibration / refinement scratch outputs
Parameters_steel_FF.txt
calibrant_screen_out.csv
hkls.csv
refined_MIDAS_params_ff.txt
ff_*.tiff.checkpoint.txt
ff_*.tiff.caked.hdf
ff_*_spline_only_map.png
ff_*_stage*.png
test6.
refinement/
scratch/
unknown_integration/

# Benchmark suite (APEXA-Bench) IS released here alongside the framework it drives.
# Track code + task/scenario defs + reference ground truth + the safety traces that
# back the paper; exclude the multi-GB regenerable detector data and loose scratch.
benchmark/detector_zoo/ge/
benchmark/detector_zoo/pilatus/
benchmark/detector_zoo/varex_aero/
benchmark/detector_zoo/varex_distortion/
benchmark/results/day2/
benchmark/results/smoke_*/
benchmark/results/*.json
benchmark/results/*.log
benchmark/**/*.bak
# but DO ship the NIST reference structure (overrides the generic CeO2_*.cif rule)
!benchmark/detector_zoo/CeO2_NIST_5p41165.cif

# Internal release admin paperwork (Argonne Inventor Portal disclosures,
# DOECode submission drafts, IPDG correspondence) — not for public repo
release/

# Tutorials-demo: defer new presentation generators/decks to a later release
tutorials-demo/apexa_aps_presentation.py
tutorials-demo/apexa_architecture_diagrams.py
tutorials-demo/APEXA_APS_Presentation.pptx
tutorials-demo/APEXA_Architecture_Detailed.pptx
tutorials-demo/APEXA_Architecture_HighLevel.pptx
# OSF upload staging (data mirror; not tracked)
osf_bundle/

# OSF API token (never commit) — inline comments break .gitignore patterns,
# so the comment MUST stay on its own line above the pattern
.osfcli.config
